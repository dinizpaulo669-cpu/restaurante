# Variáveis de ambiente necessárias para o Render

# Banco de dados (obrigatório)
SUPABASE_URL=postgresql://user:password@host:port/database

# Configurações do servidor
NODE_ENV=production
PORT=10000

# Segurança de sessão (gerado automaticamente pelo Render)
SESSION_SECRET=your-secret-key-here

# Opcional: Para autenticação customizada
# CUSTOM_AUTH_DOMAIN=your-app-domain.com